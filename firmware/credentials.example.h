#pragma once

// Copy this file to credentials.h and enter the local deployment values.
// credentials.h is intentionally excluded from Git.
constexpr char WIFI_SSID[] = "YOUR_WIFI_SSID";
constexpr char WIFI_PASSWORD[] = "YOUR_WIFI_PASSWORD";
constexpr char MQTT_SERVER[] = "192.168.1.100";
constexpr int MQTT_PORT = 1883;
